# object_detection
Real-time Object Detection system using deep learning to detect objects from images, videos, and webcam streams. Built with Python, OpenCV, and pre-trained models. Includes a simple UI for easy interaction and supports live detection with high accuracy.
